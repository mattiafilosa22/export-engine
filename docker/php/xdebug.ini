; Xdebug 3 — dev-only. mode=off means ~zero overhead; enable step debugging at
; runtime with XDEBUG_MODE=debug (the env var takes precedence over this ini).
xdebug.mode = off
xdebug.start_with_request = yes
xdebug.client_host = host.docker.internal
xdebug.client_port = 9003
xdebug.log_level = 0
